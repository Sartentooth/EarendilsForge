---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contacto">
<section class="max-w-xl mx-auto my-10 px-4 py-8 rounded-lg shadow-lg" style="background: var(--gradient-surface); color: var(--color-text);">
    <h1 class="text-4xl font-bold text-center mb-2 text-gradient" style="font-family: var(--font-title);">Contactanos</h1>
    <p class="text-center mb-6" style="color: var(--color-text-muted); font-family: var(--font-body);">
      Escribinos para coordinar tu pedido,
      consulta o simplemente compartir tu visón de lo que necesitas.
    </p>

    <form name="contacto" method="POST" data-netlify="true" class="flex flex-col gap-4">
      <label class="flex flex-col">
        <span class="font-semibold mb-1">Nombre completo</span>
        <input
          type="text"
          name="nombre"
          placeholder="Ej: Eärendil Smith"
          required
          minlength="3"
          pattern="[A-Za-zÁÉÍÓÚÑáéíóúñ\s]{3,}"
          class="p-2 rounded-md bg-theme-bg text-theme-text focus:outline-none focus:ring-2 focus:ring-amber-500"
        />
      </label>

      <label class="flex flex-col">
        <span class="font-semibold mb-1">Email</span>
        <input
          type="email"
          name="email"
          placeholder="tuemail@ejemplo.com"
          required
          type="email"
          pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
          class="p-2 rounded-md bg-theme-bg text-theme-text focus:outline-none focus:ring-2 focus:ring-amber-500"
        />
      </label>

      <label class="flex flex-col">
        <span class="font-semibold mb-1">Teléfono</span>
        <input
          type="tel"
          name="telefono"
          placeholder="+54 9 11 1234-5678"
          required
          pattern="\+54\s9\s\d{2,4}\s\d{4}-\d{4}"
          title="Formato: +54 9 11 1234-5678"
          class="p-2 rounded-md bg-theme-bg text-theme-text focus:outline-none focus:ring-2 focus:ring-amber-500"
        />
      </label>

      <label class="flex flex-col">
        <span class="font-semibold mb-1">Tu consulta</span>
        <textarea
          name="mensaje"
          rows="5"
          placeholder="Contanos tu idea, proyecto o duda..."
          required
          minlength="10"
          title="Por favor, ingresa al menos 10 caracteres."
          class="p-2 rounded-md bg-theme-bg text-theme-text focus:outline-none focus:ring-2 focus:ring-amber-500 resize-y"
        ></textarea>
      </label>

      <div class="flex justify-between gap-4 mt-4">
        <button
          type="submit"
          class="flex-1 py-2 rounded-md bg-amber-500 hover:bg-amber-600 text-theme-text font-semibold transition"
        >
          Enviar
        </button>
        <button
          type="reset"
          class="flex-1 py-2 rounded-md bg-gray-600 hover:bg-gray-700 text-theme-text font-semibold transition"
        >
          Reset
        </button>
      </div>
    </form>

    <div class="text-center mt-8">
      <p class="text-gray-300 mb-2">¿Preferís charlar por WhatsApp?</p>
      <a
        href="https://wa.me/+5491169645793"
        class="text-amber-400 font-semibold hover:underline"
        target="_blank"
      >
        Hablá con nosotros
      </a>
    </div>
  </section>
</BaseLayout>
